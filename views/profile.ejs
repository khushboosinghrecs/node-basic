<!DOCTYPE html>
<html lang ="en">
    <head>
        <title>profile page</title>
    </head>
    <body>
        <h1> Welcome <%= user.name %></h1>

        <h1> email <%= user.email %></h1>
        <ul> skills <% user.skills.forEach((item)=>{ %>
        <li><%= item %></li>

    <% }) %></ul>

    </body>
</html>